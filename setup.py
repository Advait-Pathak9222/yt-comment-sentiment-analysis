from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small cchrome plugin to detect youtube comment sentiments',
    author='A small chrome plugin to detect youtube comment sentiments',
    license='',
)
